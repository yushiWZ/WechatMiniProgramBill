/**
 * ============================================================
 * 智能记账本 - 全局配置文件
 * ============================================================
 *
 * 本文件集中管理后端服务的所有配置项。
 * 在正式上线（生产环境）前，请务必将占位值替换为真实的密钥和凭证，
 * 否则将面临安全风险（如 JWT 伪造、数据泄露等）。
 *
 * 【安全提示】
 *   - 生产环境中建议通过环境变量注入敏感信息，而非硬编码在代码中
 *   - jwtSecret 应使用高强度随机字符串，避免被暴力破解
 *   - appId / appSecret 是微信小程序的核心凭证，切勿提交到公开仓库
 */
module.exports = {
  // HTTP 服务监听端口号，默认 3000
  // 微信小程序要求线上环境使用 HTTPS 443 端口，开发调试时可使用任意端口
  port: 3000,

  // 微信小程序 AppID
  // 在微信公众平台 → 开发管理 → 开发设置 中获取
  // 以 "wx000" 开头的占位值表示使用测试号模式，此时跳过微信接口调用，
  // 直接将前端传入的 code 作为 openid，方便本地开发调试
  appId: 'wx0000000000000000', // 替换为你的小程序 AppID（测试号可用任意值）

  // 微信小程序 AppSecret
  // 与 AppID 配合使用，用于调用微信 jscode2session 接口换取用户 openid
  appSecret: '00000000000000000000000000000000', // 替换为你的小程序 AppSecret

  // JWT（JSON Web Token）签名密钥
  // 用于签发和验证登录令牌，有效期在登录时设置为 7 天（见 routes/auth.js）
  // 生产环境应替换为高强度随机字符串，防止 Token 被伪造
  jwtSecret: 'expense-tracker-jwt-secret-key-2024',

  // SQLite 数据库文件存储路径（相对于 backend/ 目录）
  // 使用 sql.js 内存数据库 + 文件持久化方案，数据定期落盘到此文件
  dbPath: './database.sqlite'
};
